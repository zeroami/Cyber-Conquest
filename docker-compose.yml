version: "3.8"

services:
  cyber-conquest:
    build: .
    container_name: cyber-conquest
    restart: unless-stopped
    ports:
      - "8501:8501"
    volumes:
      - ./progress.json:/app/progress.json
